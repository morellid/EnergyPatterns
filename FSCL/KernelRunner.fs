namespace FSCL

open Cloo
open Microsoft.FSharp.Quotations

type KernelRunner() =
    static member Run(expr: Expr) =
        5
    (*
        let kernel = KernelBinding.ConvertToCLKernel(expr)
        match kernel with
        | Some(kernelSource, args) ->
            // Create buffers for each array arg
            for *)
            
            
            

